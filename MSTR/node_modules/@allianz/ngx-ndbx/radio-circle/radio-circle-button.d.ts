import { NxRadioCircleComponent } from './radio-circle.component';
import { ElementRef, OnChanges } from '@angular/core';
export declare abstract class NxRadioCircleButton implements OnChanges {
    /** @docs-private */
    protected radioCircle: NxRadioCircleComponent;
    /** @docs-private */
    id: number;
    /** Sets the label displayed below the circle. */
    label: string;
    /** Sets an additional hint displayed below the label. */
    hint: string;
    /** Sets the value used in the model. */
    value: any;
    /** @docs-private */
    labelElement: ElementRef;
    constructor(
    /** @docs-private */
    radioCircle: NxRadioCircleComponent);
    ngOnChanges(): void;
    /** Whether the item is preselected. */
    checked: boolean;
    /** @docs-private */
    readonly circleButtonId: string;
    /** @docs-private */
    readonly radioCircleId: string;
}
