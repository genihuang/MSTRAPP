import { AfterViewInit } from '@angular/core';
import { NxRadioCircleButton } from '../radio-circle-button';
import { NxRadioCircleComponent } from '../radio-circle.component';
/**
 * @deprecated Use `nx-circle-toggle` instead
 * @deletion-target 8.0.0
 */
export declare class NxRadioCircleSvgButtonComponent extends NxRadioCircleButton implements AfterViewInit {
    /** Sets the SVG used for the not-checked state. */
    svg: string;
    /** Sets the SVG used for the checked state. */
    svgChecked: string;
    private _hover;
    /** @docs-private */
    onMouseEnter(): void;
    /** @docs-private */
    onMouseLeave(): void;
    constructor(radioCircle: NxRadioCircleComponent);
    ngAfterViewInit(): void;
    /** @docs-private */
    readonly svgUrl: string;
}
