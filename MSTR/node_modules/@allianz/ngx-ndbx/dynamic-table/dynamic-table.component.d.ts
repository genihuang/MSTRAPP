import { ElementRef, EventEmitter, OnInit, ChangeDetectorRef } from '@angular/core';
import { NxDisplayedColumns } from './displayedColumns';
import { TableDataSource } from './tabledata-source';
export declare class NxDynamicTableComponent implements OnInit {
    private el;
    private _changeDetectorRef;
    private _tableStyles;
    private _dataChange;
    private _dataSource;
    private _data;
    private _classNames;
    private _displayedColumns;
    private _columnKeys;
    /** Sets the data that it will show in the table. */
    data: any[];
    /** Sets the name order and type of columns. */
    displayedColumns: NxDisplayedColumns[];
    /** @deprecated The default and light header styling will be removed.
     * @deletion-target 8.0.0
    */
    styles: string;
    /** An event is dispatched when a row is clicked. */
    nxRowClick: EventEmitter<{}>;
    /** @docs-private */
    readonly dataSource: TableDataSource;
    /** @docs-private */
    readonly classNames: string;
    /** @docs-private */
    readonly columnKeys: string[];
    constructor(el: ElementRef, _changeDetectorRef: ChangeDetectorRef);
    ngOnInit(): void;
    /** @docs-private */
    handleRowClick(row: Object): void;
    /** @docs-private */
    isNumeric(element: NxDisplayedColumns): boolean;
}
