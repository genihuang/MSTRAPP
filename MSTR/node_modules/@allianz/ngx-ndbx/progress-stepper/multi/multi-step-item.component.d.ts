import { ChangeDetectorRef } from '@angular/core';
import { NxStepComponent, NxProgressStepperDirective } from './../progress-stepper.component';
import { ErrorStateMatcher } from '@allianz/ngx-ndbx/utils';
/** @docs-private */
export declare class NxMultiStepItemComponent extends NxStepComponent {
    private _changeDetectorRef;
    /** The state of the step. */
    state: string;
    /** The label of the step. */
    label: string;
    /** Sets the index of the selected step. */
    index: number;
    private _index;
    /** Sets the selected step. */
    selected: boolean;
    private _selected;
    /** Sets the active step. */
    active: boolean;
    private _active;
    /** Sets the last step. */
    last: boolean;
    private _last;
    /** Sets the first step. */
    first: boolean;
    private _first;
    /** Whether the step is completed or not. */
    stepCompleted: boolean;
    private _completed;
    constructor(_changeDetectorRef: ChangeDetectorRef, stepper: NxProgressStepperDirective, _errorStateMatcher: ErrorStateMatcher);
}
