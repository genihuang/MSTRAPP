import { AfterContentInit, ChangeDetectorRef, EventEmitter, OnInit, QueryList } from '@angular/core';
import { NxTabLabelDirective } from './tab-label';
/** @docs-private */
export declare class NxTabHeaderComponent implements OnInit, AfterContentInit {
    private _changeDetectorRef;
    private _keyManager;
    private _selectedIndex;
    selectedIndex: number;
    focusIndex: number;
    private _autoselect;
    autoselect: boolean;
    readonly selectFocusedIndex: EventEmitter<number>;
    readonly indexFocused: EventEmitter<number>;
    labels: QueryList<NxTabLabelDirective>;
    constructor(_changeDetectorRef: ChangeDetectorRef);
    ngOnInit(): void;
    ngAfterContentInit(): void;
    private _isValidIndex;
    /**
     * Handles keyboard inputs on the labels
     * If autoselect is enabled the tab gets changed immediately
     * If autoselect is disabled only the focus changes but the user still has to select the item
     * by himself
     */
    handleKeydown(event: KeyboardEvent): void;
}
