import { NxTabGroupBase } from './tab-group-base';
import { TemplatePortal } from '@angular/cdk/portal';
import { OnChanges, OnDestroy, OnInit, SimpleChanges, TemplateRef, ViewContainerRef } from '@angular/core';
import { Subject } from 'rxjs';
export declare class NxTabComponent implements OnInit, OnChanges, OnDestroy {
    private _viewContainerRef;
    private _tabGroup;
    private _label;
    /** The portal that will be the hosted content of the tab */
    private _contentPortal;
    /** Emits whenever the internal state of the tab changes. */
    readonly _stateChanges: Subject<void>;
    /** Sets the label of the tab shown in the tablist. */
    label: string;
    /** @docs-private */
    readonly content: TemplatePortal | null;
    /** Template inside the NxTab view that contains an `<ng-content>`. */
    _implicitContent: TemplateRef<any>;
    /**
     * Whether the tab is currently active.
     */
    isActive: boolean;
    constructor(_viewContainerRef: ViewContainerRef, _tabGroup: NxTabGroupBase);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
}
