import { ChangeDetectorRef } from '@angular/core';
export declare class NxTabNavBarComponent {
    private _changeDetectorRef;
    private _negative;
    /** Whether the tab nav bar has negative styling. */
    negative: boolean;
    constructor(_changeDetectorRef: ChangeDetectorRef);
}
export declare class NxTabLinkDirective {
    private _tabNavBar;
    private _active;
    /** Whether the tab link is active and has the active styling. */
    active: boolean;
    constructor(_tabNavBar: NxTabNavBarComponent);
}
